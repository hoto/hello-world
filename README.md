# Docker + nodejs hello world

* Run Docker

        # in dev mode
        docker-compose up

        # in production mode
        docker-compose -f docker-compose.yml up

* Open in browser

> localhost:3000
> localhost:3000/test
